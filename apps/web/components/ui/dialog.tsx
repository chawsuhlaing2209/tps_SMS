'use client'

/** @deprecated Import from `@/components/pds` — `Modal` is the canonical dialog surface. */
export {
  Modal as Dialog,
  ModalTrigger as DialogTrigger,
  ModalClose as DialogClose,
  ModalPortal as DialogPortal,
  ModalOverlay as DialogOverlay,
  ModalContent as DialogContent,
  ModalHeader as DialogHeader,
  ModalTitle as DialogTitle,
  ModalDescription as DialogDescription,
  ModalCloseButton as DialogCloseButton,
  ModalBody as DialogBody,
  ModalFooter as DialogFooter,
  ModalFooterStart as DialogFooterStart,
  ModalFooterActions as DialogFooterActions,
} from '../pds/composites/modal'
